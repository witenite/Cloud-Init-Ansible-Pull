- hosts: localhost
  connection: local
  become: true
  tasks:
    - include_tasks: tasks/cron-user.yml
    - include_tasks: tasks/user-witenite.yml

  - name: install packages
    package:
      name:
        - htop
        - tmux
        - vim-nox
        - avahi-daemon
        - vim
